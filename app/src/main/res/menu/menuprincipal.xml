<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <item
        android:id="@+id/action_setting"
        android:icon="@drawable/ic_baseline_menu_24"
        android:orderInCategory="100"
        android:title="Camara"
        app:showAsAction="always">

        <menu>
            <item
                android:id="@+id/bt_camara"
                android:icon="@android:drawable/ic_menu_camera"
                android:title="Camara" />
            <item
                android:id="@+id/bt_libros"
                android:icon="@android:drawable/ic_input_get"
                android:title="Best Seller" />
            <item
                android:id="@+id/bt_precios"
                android:icon="@android:drawable/ic_menu_search"
                android:title="Grafico" />
        </menu>

    </item>


</menu>

